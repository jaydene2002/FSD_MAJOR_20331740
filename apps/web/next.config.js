/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['plus.unsplash.com', 'images.unsplash.com'], // Add both Unsplash domains
    },
  };
  
  export default nextConfig;